# Binaries
/motzworks
/bin/
*.exe

# Local config & secrets
config.yaml
.env
*.key

# Frontend
node_modules/
# Vite caches; the built dashboard (internal/web/dist) IS committed so the
# Go binary always has assets to embed.
web/ui/.vite/

# Editor/OS
.DS_Store
*.swp
